/*#include <stdio.h>  

int main() {
    // 无限循环  
    while (1) {
        // 使用一个无意义的计算来占用CPU资源  
        // 注意：这里只是简单地执行了一个无用的操作，实际中可以添加更复杂的计算  
        volatile double x = 0.0;
        for (volatile long long i = 0; i < 10000000000; ++i) {
            x = x + 3.141592653589793 * 2.718281828459045;
        }
        // 理论上，上面的计算不会产生任何有用的结果，因为x的值在每次循环迭代后都被覆盖  
        // 但是，它确实会消耗CPU资源  

        // 如果需要，可以在这里添加代码来检查或报告CPU的使用情况  
        // 但通常这种程序是简单的，只是为了尽可能占用CPU  
    }

    return 0; // 这行代码实际上永远不会被执行  
}*/